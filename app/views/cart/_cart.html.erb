<%= turbo_frame_tag "cart" do %>
    <h1>Cart</h1>
    <% if @cart.order.count > 0 %>
        <%= render "cart/order" %>
        <p>Total: <%= cart.total %></p>
    <% else %>
        <p>Your cart is empty</p>
    <% end %>
<% end %>